class Urchin { //<>//
  
PVector urchinPos;

//creates variables for the collectable urchins (dose not work)
  Urchin(float xPos, float yPos) { //<>//
    urchinPos.x = xPos;
    urchinPos.y = yPos;
  }
  
//displays the collectable urchins
 void display() { //<>//
    //image(urchin, urchinPos.x, urchinPos.y, 30, 30);
  }
}
